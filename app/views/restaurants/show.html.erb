<h1>Restaurants#show</h1>
<p>Find me in app/views/restaurants/show.html.erb</p>

<%= @restaurant.address %>
<%= @restaurant.category %>
<%= @restaurant.phone_number %>

<p><%= link_to 'Edit', edit_restaurant_path(@restaurant), class: 'btn btn-warning'%></p>
<p><%= link_to "Back to restaurants", restaurants_path, class: 'btn btn-warning' %></p>
<p><%= link_to "Delete this restaurants", restaurant_path(@restaurant), data: { turbo_method: :delete, turbo_confirm: "are you sure?" }, class: 'btn btn-warning'%></p>
